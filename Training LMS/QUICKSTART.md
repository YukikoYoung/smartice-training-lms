# 🚀 快速开始指南

## 📋 前置要求

- Python 3.8+
- pip3

## 🏃 快速启动（3步）

### 1. 安装依赖

```bash
cd backend
pip3 install -r requirements.txt
```

### 2. 启动后端服务

```bash
cd backend
python3 main.py
```

或使用启动脚本：
```bash
cd backend
./run.sh
```

启动后访问：
- 后端API: http://localhost:8000
- API文档: http://localhost:8000/docs

### 3. 打开前端测试页面

使用浏览器打开：
```
frontend-test/index.html
```

或使用VS Code Live Server插件。

## 🧪 测试账号

| 角色 | 用户名 | 密码 | 职级 |
|------|--------|------|------|
| 运营负责人 | admin | admin123 | L5+ |
| 区域经理 | regional_mgr | 123456 | L5 |
| 店长 | store_mgr | 123456 | L4 |
| 厨师长 | chef | 123456 | L4 |
| 服务员 | waiter001 | 123456 | L1 |

## 📁 项目结构

```
Training LMS/
├── backend/              # 后端代码
│   ├── app/
│   │   ├── core/        # 核心配置
│   │   ├── models/      # 数据模型 ✅
│   │   ├── schemas/     # 数据验证
│   │   ├── routers/     # API路由
│   │   └── services/    # 业务逻辑
│   ├── scripts/         # 工具脚本
│   ├── main.py          # 入口文件
│   ├── .env             # 环境配置
│   └── run.sh           # 启动脚本
│
├── frontend-test/       # 测试前端
│   └── index.html       # 登录测试页面
│
└── QUICKSTART.md        # 本文件
```

## ✅ 已完成功能

### 数据模型（17个表）
- ✅ User, Region, Store, Position（用户与组织）
- ✅ Course, Chapter, Content（课程体系）
- ✅ Exam, Question（考试系统）
- ✅ CourseProgress, ChapterProgress, ExamRecord, DailyQuizRecord, ValueAssessment（学习记录）

### 后端API
- ✅ 用户登录/注册
- ✅ 获取当前用户信息
- ✅ JWT Token认证

### 前端测试界面
- ✅ 登录页面
- ✅ 用户信息展示
- ✅ Token本地存储

## 🔧 常见问题

### 1. 端口被占用
```bash
# 修改端口号（main.py第94行）
uvicorn.run("main:app", host="0.0.0.0", port=8001)
```

### 2. CORS错误
确保frontend-test使用Live Server或http-server启动，不要直接用file://协议打开。

### 3. 数据库重置
```bash
cd backend
rm training_lms.db  # 删除数据库
python3 main.py     # 重新启动会自动创建
python3 scripts/init_data.py  # 初始化示例数据
```

## 📝 下一步开发

MVP剩余功能：
- [ ] 课程管理API
- [ ] 学习进度API
- [ ] 考试系统API
- [ ] 随堂检测API
- [ ] 前端完整界面
- [ ] 数据看板
- [ ] 移动端适配

## 📖 API文档

启动服务后访问：http://localhost:8000/docs

可以在Swagger UI中测试所有API接口。

---

**问题反馈**：请在GitHub Issues中提交
**技术栈**：FastAPI + SQLAlchemy + SQLite + Vanilla JS
